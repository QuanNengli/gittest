<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lzq.springmvc.dao.UserMapper">
<select id="selectByid" resultType="studentBean">
    select * from t_stu where sid=#{id}
</select>

</mapper>